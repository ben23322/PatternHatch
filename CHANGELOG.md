# 更新日志

## [0.1.39] - 2026-08-01

### 修复（TJ 平行机合成错误）

- 修复 TJ 平行机"跑旧配方/合成错误"：TJ 的 ParallelRecipeLRUCache 按内容子集
  匹配返回最近命中配方，活动槽切换、模式（配方图）切换、或槽内材料类型变化后，
  旧配方可能被新内容顺带满足导致机器跑错配方。现在检测到上述变化时自动
  清空 TJ 配方 LRU 强制重搜（与 GTCEu 侧 previousRecipe.clear 行为一致）

## [0.1.38] - 2026-08-01

### 修复（外观/日志噪音）

- 补上缺失的 pattern_hatch_overlay/overlay_front_active.png（样板仓工作状态贴图），
  消除日志中的 FileNotFound / TEXTURE_ERROR 材质报错

## [0.1.37] - 2026-08-01

### 修复（进不去存档 - 第二轮）

- 修复 "FluidTankList cannot be cast to IFluidTank" 崩溃：IMPORT_FLUIDS 能力表的
  元素类型是 IFluidTank（流体仓注册的是内部 FluidTank），0.1.36 误注册了
  FluidTankList 容器，机器成型后配方逻辑按 tank 强转导致崩溃。现在注册
  0 容量惰性 FluidTank，结构计数有效且完全惰性（不能存任何流体）

## [0.1.36] - 2026-08-01

### 修复（进不去存档）

- 修复 "Attempted to add item handler ... twice" 崩溃：样板仓向 IMPORT_ITEMS /
  IMPORT_FLUIDS 能力表注册空处理器时，之前复用同一个静态实例，同一台机器上
  有多个样板仓（或多处注册）时 ItemHandlerList 构造直接抛异常导致存档无法加载。
  现在每次注册 new 独立实例，多仓共存正常

## [0.1.35] - 2026-08-01

### 新功能：TJ 平行机样板仓支持（P0）

- 样板仓现在可以**完全替代输入总线/输入仓**放进 TJ 平行机（28 台平行机结构通用槽位，
  无需再留真总线）：registerAbilityFor 把空物品/流体处理器登记进 IMPORT_ITEMS /
  IMPORT_FLUIDS 能力表，结构检查与 busCount 均计入样板仓
- PatternMachineLogic 新增 TJ 分支：TJ 平行机空闲时按 36 槽优先级选活动槽，
  复用现有摊平缓存视图 + 切片记账（并行不漏扣/不增产）+ 催化剂 + 虚拟电路
- 三个 TJ 输入口子重定向：getImportItemInventory / getImportFluidTank /
  getInputBus 在有活动样板槽时返回该槽视图（distinct 也走样板），空闲回退原输入，
  手动合成照旧；保留 5 秒活动槽保持锁与空闲自动弹回 AE
- 配置开关：config/patternhatch.cfg -> tjParallel.enabled（默认 true），
  出问题可一键关闭 TJ 支持
- 保留全部崩溃防护（getInputBus 越界、distinct 无总线自动关闭）

## [0.1.34] - 2026-08-01

### 修复（TJ 平行机崩服 - 第二轮）

- 新增 MixinTJAbstractParallelWorkableHandler：distinct 模式但输入总线为 0 时
  每 tick 在 update 入口自动关闭 distinct（写入 NBT 持久化）。TJ 允许结构在
  无物品输入总线时成型（纯流体配方图 minInputs=0），且 NBT 加载不会像
  setDistinct 那样拦截无总线开启 distinct，导致 checkRecipeInputsDirty 对
  长度 0 的矩阵取 [0] 崩服。修复后机器退回合并输入路径安静待机，不再崩服。
- 保留 0.1.33 的 getInputBus 空列表防护作为第二道保险

## [0.1.33] - 2026-08-01

### 修复（服务器）

- 新增 `debug.enabled` 配置（默认 false）：所有 `[PatternHatch]` 调试日志
  （machine setup / pushPattern / M3 select / checkRecipe 等）默认关闭，
  不再在服务器控制台刷屏；重要警告（缺材料、缓存丢弃）仍会打印。
  需要排查问题时在 config/patternhatch.cfg 中把 `debug:enabled` 改为 true
- 新增 TJ 平行机崩溃保护（MixinTJMultiblockControllerBase）：
  TJ 的平行化学反应釜等在 distinct 模式下，结构里没有任何输入总线时
  （例如输入总线位置放了样板仓等无输入总线能力的部件），TJ 的
  getInputBus(0) 会直接抛 IndexOutOfBoundsException 崩服。
  现在空列表时返回空物品处理器，机器安静等待输入而不崩溃。
  注：崩溃栈中没有任何 patternhatch 代码——TJ 控制器不继承
  RecipeMapMultiblockController，样板仓的 mixin 在 TJ 机器上不生效，
  此崩溃是 TJ 自身的空列表未保护问题，本补丁仅做防御

### 外观

- tooltip 重写：标题改为 `IV 样板输入总成`（IV 深蓝 §1、名称水蓝 §b），
  正文按新文案展示（为多方块机器提供物品/流体输入、直接对接 ME 网络、
  【特性说明】三项，破折号蓝色、标题金色）

## [0.1.32] - 2026-08-01

### 修复（并行增产/残余的真正根因）

- FlattenedCacheView 改为按构造快照为每个切片记账"剩余可抽取量"：
  之前并行大批量（跨多个 64 切片）时，模拟抽取能看到满额、真实抽取却因切片
  偏移失效而漏扣（如 160 钢只抽走 96），配方引擎仍按 20x 开工 → 白出 8 个、
  剩余 64 钢触发第二批 8x → 下单 20 出 28。现在真实抽取量与模拟一致，
  160 钢实扣 160，缓存清零，不再多跑第二批（普通输入总线手动合成不受影响）
- 活动槽保持锁：缓存被吃空的间隙锁定活动槽 5 秒，机器只等 AE 推下一批，
  不会回退去吃普通输入总线/输入仓的存货（防另一条增产路径）

### 外观

- tooltip 精简为 13 行（保留机器类型/出现电压/核心功能/注意事项/附属信息，
  砍掉冗长描述，红标题/蓝破折号/金色数字/深蓝 IV 配色保留）

## [0.1.31] - 2026-08-01

### 修复

- 普通输入总线/输入仓手动合成恢复：撤销完全隔离（无活动样板时机器使用普通
  输入），有活动样板时仍走样板缓存
- 右侧缓存栏跟随客户端语言：服务端只同步结构化数据，客户端本地化显示名
  （修复独立服务器上右侧变英文的问题）

### 外观

- tooltip 改为维基风格（红标题/蓝破折号/金色数字/深蓝 IV），去掉"开发作者"，
  保留 所属模组

## [0.1.30] - 2026-08-01

### 配置

- `cache.busyThreshold` 与 `cache.cacheCapItems` 默认值均为最大（21 亿）：
  并行批次完全跟随机器电压、缓存无上限（超产门控暂缓，可自行在 cfg 调小
  busyThreshold 重新启用）

## [0.1.29] - 2026-08-01

### 修复（并行机超产/无中生有）

- isBusy 门控：单槽缓存达到一批量（cfg `cache.busyThreshold`，默认 432=48×9）
  时报告忙，AE 合成 CPU 暂停推料；机器吃完一批、产物回流后再推。
  治本：并行机囤料期间 CPU 不再超前推料（此前 3100 次推送即超前推料导致
  产出超过下单量，普通机器 1:1 无囤料所以没此问题）

## [0.1.28] - 2026-08-01

### 修复（尾料残留）

- 空闲自动弹回：机器无活动槽且缓存有残余（如 <9 凑不成批的尾料）超过 5 秒时，
  自动把残余退回 AE，不再需要手动按"弹回AE"
- 缓存上限默认改回 21 亿（cfg 仍可调）

## [0.1.27] - 2026-08-01

### 修复（超产/残余）

- 完全隔离：样板仓装有样板时，机器输入**永不回退**到普通输入仓/总线
  （此前缓存被吃空的间隙机器会"顺嘴"吃总线里的料导致总消耗超过发配量，
  这是只有带输入总线的并行机才出现"古怪"超产的原因）；无样板时手动合成照常

## [0.1.26] - 2026-08-01

### 修复（缓存堆积/残余）

- 每个样板槽物品缓存增加上限（cfg `cache.cacheCapItems`，默认 1000/槽）：
  AE 推到上限即暂停推送，机器吃完一批、产物回流后再推，缓存不再无限堆积
  （针对"输出走 ME 输出总线、缓存无上限导致吃完全部推料留下残余"的问题）
- 空闲时每 200 tick 打印各槽缓存残余，便于核对

### 外观

- tooltip 重新排版：标题加粗白色、描述灰色、IV 深蓝、流体天蓝、
  "完全隔离"金色加粗，段落间加空行，统一左对齐

## [0.1.25] - 2026-08-01

### 修复（超产）

- 修复摊平视图超产 bug：切片映射改为构造时固定一次，抽取过程中不随源数量
  变化移位（之前一次抽取只抽走部分、剩余被下一批重复使用导致超产）

### 外观

- tooltip：IV 深蓝色、流体天蓝色、"忽略其他输入仓…"前加"#完全隔离："

## [0.1.24] - 2026-08-01

### 新功能

- 催化剂槽 4 → 9 格
- 电路槽 1 → 2 格：槽 0 为虚拟电路（GhostCircuitWidget，点击/滚轮调配置，
  无需手动放置编程电路，参考 GT 输入总线）；槽 1 为实体电路（可选）
- 物品 tooltip：机器类型/等级/功能说明（中英文）

### 诊断

- pushPattern 增加缓存总量日志；恢复 setup 配方输入与实际视图日志，
  跟踪大量下单时的超产来源

## [0.1.23] - 2026-08-01

### 修复（启动配置）

- 修正 mixin 配置（移除已删除的诊断类引用，避免启动崩溃）；
  活动槽视图摊平 ≤64 一叠（模拟总线形态），尝试恢复并行机并行

## [0.1.22] - 2026-08-01

### 修复（恢复并行）

- 活动槽视图把缓存摊平成 ≤64 一叠的多槽形态，完全模拟普通输入总线：
  Gregicality 并行机对单槽超大堆叠（9000 一叠）的并行消耗异常，摊平后走
  与总线一致的路径，并行恢复正常

## [0.1.21] - 2026-08-01

### 诊断（并行）

- 恢复活动槽模式 1 倍运行（保证可玩），同时加回 matchesFound 探针（唯一
  重载、完整描述符）：对比 matchesFound(false)（搜索/缓存）与
  matchesFound(true)（消耗）的结果，定位并行 48x 消耗失败的精确分支

## [0.1.20] - 2026-08-01

### 修复（启动崩溃）

- 移除 Recipe.matchesFound 探针（该崩溃为 JEI + Java 25 的 fastutil 哈希
  ArrayIndexOutOfBounds，非 mixin 报错；为排除关联一并移除探针）

## [0.1.19] - 2026-08-01

### 诊断（恢复并行）

- 恢复 Gregicality 并行机在样板模式下的并行放大（移除 1x 强制）
- setup HEAD 日志打印并行配方的输入清单（recipeInputs），并新增
  `Recipe.matchesFound`（handler 版，完整描述符）探针，定位并行消耗失败点

## [0.1.18] - 2026-08-01

### 诊断（恢复并行）

- setup 日志打印 `getInputInventory()` 的真实返回内容，确认并行消耗失败时
  机器实际看到的是活动槽视图还是普通总线视图，为恢复并行放大定位根因

## [0.1.17] - 2026-08-01

### 修复（并行机配方不启动）

- 修复：Gregicality 大型并行机（如 LargeBenderAndForming，并行 48）在样板仓
  活动槽驱动时禁用并行放大——并行机会按输入量把配方放大成 48x，而样板仓每次
  只推一份材料，放大后消耗匹配失败导致机器卡死。现在活动槽模式按原配方 1 倍
  执行，手动模式保留并行
- 移除 v0.1.16 的 Recipe 探针（签名与 fork 另一套 matchesItems(List) 冲突
  导致启动崩溃），改为独立可选配置加载 GA 并行机 mixin

## [0.1.16] - 2026-08-01

### 诊断

- 直接探测 `Recipe.matchesFound` / `matchesItems`：失败时打印配方输入列表与
  实际视图内容，定位并行机（48x 压缩板）消耗匹配失败的具体成分

## [0.1.15] - 2026-08-01

### 诊断

- 分层日志：区分失败发生在 `MultiblockRecipeLogic.setupAndConsumeRecipeInputs`
  （checkRecipe 层）还是 `AbstractRecipeLogic`（能量/消耗层）：
  新增 `super.setupAndConsume -> true/false` 日志；移除诊断中对 checkRecipe
  的自调用（避免副作用干扰）

## [0.1.14] - 2026-08-01

### 诊断

- setup 失败日志增加活动槽视图实际内容（各槽物品、电路槽、是否有活动槽），
  用于定位 LargeBenderAndForming 并行机（48x 压缩板配方）消耗失败的原因

## [0.1.13] - 2026-08-01

### 诊断

- M3 选槽日志增加机器类名、工作台类名、结构状态、workingEnabled、
  progress/maxProgress，用于确认机器配方逻辑为何不进入搜索（TJ 机器用的是
  独立的 BasicRecipeLogic，与 GTCEu 的 MultiblockRecipeLogic 不同）

## [0.1.12] - 2026-08-01

### 修复/诊断

- 活动槽切换时清除机器的配方 LRU 缓存并强制重搜：避免命中与活动槽内容匹配的
  旧配方（如 22x/48x 压缩钢板）导致机器不启动
- 增强诊断：打印机器 setup 时的 checkRecipe 结果、机器类名与 LRU 缓存条数，
  定位自定义机器对配方的前置校验
- GUI："弹回AE"按钮移到编程电路仓右边

## [0.1.11] - 2026-08-01

### 修复（启动崩溃）

- 修复游戏无法启动：`MixinMultiblockRecipeLogicInput` 之前用 `@Shadow` 定位
  继承自父类 `MTETrait` 的 `metaTileEntity` 字段，Mixin 无法解析继承字段导致
  `MultiblockRecipeLogic` 类加载失败、GT 崩溃。已改用公开的
  `getMetaTileEntity()` 访问器

## [0.1.10] - 2026-08-01

### 修复（配方不启动）

- 修复：带电路（如 9 号）配方不启动。诊断确认机器输入视图是普通总线
  （10 槽/1 罐）而非活动槽视图——部分机器重写了控制器的
  `getInputInventory`/`getInputFluidInventory`，绕过了基类 mixin。
  现在在**工作台层**（`MultiblockRecipeLogic`）重定向输入视图，所有多方块
  （含重写 getter 的机器）都会看到活动槽缓存 + 催化剂 + 电路板仓

### 新功能（弹回 AE）

- GUI 新增"弹回AE"按钮：把 36 个样板槽缓存中的物品/流体全部送回 ME 网络
  （放不下的留在缓存里），避免拆方块时几万物品掉落导致卡顿

## [0.1.9] - 2026-08-01

### 诊断

- 增加配方启动断点日志：机器 `findRecipe` 返回值、`setupAndConsumeRecipeInputs`
  结果与能量、活动槽选中时的电路槽物品与能量，用于定位"找到配方但不启动"
  的具体原因（电路 0 号可用、非 0 号不可用）

## [0.1.8] - 2026-08-01

### 修复

- 修复：带编程电路的配方（如 9 号电路做压缩钢板）机器不识别。根因是机器的
  "优化哈希查找"（`findWithHashMap`）用忽略 NBT 的 `MapItemStackIngredient`
  做键，而编程电路的配置存在 NBT（`Configuration`）里，哈希路径匹配不上；
  活动槽搜索用的是线性查找所以能找到。现在样板仓模式下强制机器改用与
  活动槽一致的线性查找，电路配方正常识别

## [0.1.7] - 2026-08-01

### 修复

- 修复：普通输入总线/输入仓手动合成不可靠的问题。输入视图 mixin 之前依赖
  `cir.getReturnValue()`（HEAD 注入时恒为 null），导致无活动槽时回退成了
  空库存；现在直接读取控制器真实的普通输入库存字段，手动合成稳定可用
- NAE2 多功能样板工具增加调试日志（打开工具 / 绑定样板仓），便于定位
  左侧样板面板显示问题

## [0.1.6] - 2026-08-01

### 修复

- 修复：装上样板仓后多方块结构无法成型。TJ fork 的结构判定
  （`abilityPartPredicate`）只认 `IMultiblockAbilityPart.getAbility()`，
  样板仓已恢复该接口声明（仅声明能力，不把缓存注入机器普通输入，保持不串料）
- 修复：NAE2 多功能样板工具在接口视图下按按钮导致服务端
  `ClassCastException` 崩溃、左侧样板区无内容。样板库存改用 AE2
  `AppEngInternalInventory`（36 槽 × 每槽 1 个），与 AE 接口样板槽语义一致

## [0.1.5] - 2026-08-01

### 修复（普通机器不能合成）

- 修复：机器装上样板仓后，普通输入总线/输入仓完全失效的问题。现在没有活动槽
  时会自动回退到机器原有输入，手动放入物品/流体可正常合成
- 修复：样板仓缓存不再注册为机器的 IMPORT_ITEMS/IMPORT_FLUIDS 能力，避免手动
  合成时吞掉样板缓存里的材料（防止串料）

### 新功能（NAE2 兼容）

- 支持 NAE2 多功能样板工具（Pattern Multi-Tool）：用工具右键样板仓，
  会像打开 AE 接口一样打开工具界面，右侧直接读写样板仓的 36 个样板槽，
  放/取样板即时刷新 AE 配方
- NAE2 为可选依赖：未安装 NAE2 时该功能自动禁用，不影响其它功能

## [0.1.4] - 2026-08-01

### 修复（服务端崩溃）

- 修复服务端 tick 循环空指针崩溃：AE2 阻塞模式接口扫描相邻方块时，对普通 GT 机器
  （`MetaTileEntityHolder`）调用 `getInterfaceDuality()` 返回 null，导致
  `DualityInterface.isBusy()` 第 1228 行 NPE，服务端秒崩
- 根因：mixin 让**所有** GT 机器 holder 实现了 `IInterfaceHost`，而普通 GT 机器没有
  AE proxy（基础 `MetaTileEntity.getProxy()` 返回 null），holder 的
  `getInterfaceDuality()` 会返回 null
- 修复：holder 只实现 AE 发现样板所需的最小接口 `ICraftingProvider`，不再被 AE2
  当成"接口"遍历其邻居，配方发现/下发功能不受影响

## [0.1.3] - 2026-08-01

### 外观（GTNH 完全观感）

- 方块六面改用 GTNH（GT5-Unofficial）IV 机壳贴图，正面叠加 GTNH 样板输入总成图标
  （含流体版 `OVERLAY_ME_CRAFTING_INPUT_BUFFER`）
- 36 个样板槽改用 GTNH 样板槽背景贴图（`OVERLAY_SLOT_PATTERN_ME`）

### 修复

- 修复样板槽背景显示为黑紫的问题（GUI 贴图路径错误）
- 修复样板区格子线一直不可见的问题（同一根因）
- 样板区增加深灰底板 + 白/灰双层边框

### 其他

- GUI 所有文字统一为游戏字体渲染（白色 + 阴影）

## [0.1.2] - 2026-08-01

### 外观

- 样板区深灰底板 + 白色/灰色双层边框

### 修复

- GUI 文字统一为游戏字体渲染（阴影 + 白色），去掉标签的绿/青色

## [0.1.1] - 2026-08-01

### 缓存查看

- 右侧新增整列缓存显示（物品/流体完整列表，每行一个条目，按槽位顺序）
- 支持鼠标滚轮翻页 + 滚动条指示

## [0.1.0] - 2026-07-31

### 首个版本

- 样板仓本体：36 样板槽（只收 AE2 已编码样板）+ 每槽隔离缓存（物品 + 流体，每槽 9 罐）
- 4 催化剂槽 + 1 虚拟电路槽（notConsumed 只匹配不消耗）
- AE2 直连：样板即插即用、样板终端可见、放新样板即时刷新
- 物品/流体样板全支持（含 ae2fc 扩展样板 Cnt 数量解码）
- 多方块机器自动按样板优先级执行，槽位隔离不串料
- 方块破坏时样板、催化剂、电路、缓存全部掉落
- 组装机配方（材料数量可经 `config/patternhatch.cfg` 自定义）
